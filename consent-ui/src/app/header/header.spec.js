(function () {
    'use strict';

    describe('cmChairConsole module', function () {

        beforeEach(module('cmChairConsole'));

        describe('ChairConsole controller', function () {

            it('should ....', inject(function ($controller) {
                //spec body
                var chair_consoleCtrl = $controller('ChairConsole');
                expect(chair_consoleCtrl).toBeDefined();
            }));

        });
    });

})();
