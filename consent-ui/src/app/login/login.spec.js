(function () {
    'use strict';

    describe('cmLogin module', function () {

        beforeEach(module('cmLogin'));

        describe('Login controller', function () {

            it('should ....', inject(function ($controller) {
                //spec body
                var chair_consoleCtrl = $controller('Login');
                expect(chair_consoleCtrl).toBeDefined();
            }));

        });
    });

})();
