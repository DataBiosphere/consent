(function () {
    'use strict';

    describe('cmFooter module', function () {

        beforeEach(module('cmFooter'));

        describe('Footer controller', function () {

            it('should ....', inject(function ($controller) {
                //spec body
                var footerCtrl = $controller('Footer');
                expect(footerCtrl).toBeDefined();
            }));

        });
    });

})();
