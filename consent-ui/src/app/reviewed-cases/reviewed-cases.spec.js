(function () {
    'use strict';

    describe('cmReviewedCases module', function () {

        beforeEach(module('cmReviewedCases'));

        describe('ReviewedCases controller', function () {

            it('should ....', inject(function ($controller) {
                //spec body
                var reviewed_casesCtrl = $controller('ReviewedCases');
                expect(reviewed_casesCtrl).toBeDefined();
            }));

        });
    });

})();
