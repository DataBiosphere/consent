(function () {
    'use strict';

    describe('cmAdminConsole module', function () {

        beforeEach(module('cmAdminConsole'));

        describe('AdminConsole controller', function () {

            it('should ....', inject(function ($controller) {
                //spec body
                var admin_consoleCtrl = $controller('AdminConsole');
                expect(admin_consoleCtrl).toBeDefined();
            }));

        });
    });

})();
