(function () {
    'use strict';

    describe('cmReview module', function () {

        beforeEach(module('cmReview'));

        describe('Review controller', function () {

            it('should ....', inject(function ($controller) {
                //spec body
                var access_reviewCtrl = $controller('Review');
                expect(access_reviewCtrl).toBeDefined();
            }));

        });
    });

})();
