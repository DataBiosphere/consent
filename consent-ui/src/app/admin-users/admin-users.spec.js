(function () {
    'use strict';

    describe('cmAdminUsers module', function () {

        beforeEach(module('cmAdminUsers'));

        describe('AdminUsers controller', function () {

            it('should ....', inject(function ($controller) {
                //spec body
                var admin_usersCtrl = $controller('AdminUsers');
                expect(admin_usersCtrl).toBeDefined();
            }));

        });
    });

})();
