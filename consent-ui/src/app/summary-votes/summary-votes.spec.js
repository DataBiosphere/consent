(function () {
    'use strict';

    describe('cmSummaryVotes module', function () {

        beforeEach(module('cmSummaryVotes'));

        describe('SummaryVotes controller', function () {

            it('should ....', inject(function ($controller) {
                //spec body
                var summary_votesCtrl = $controller('SummaryVotes');
                expect(summary_votesCtrl).toBeDefined();
            }));

        });
    });

})();
