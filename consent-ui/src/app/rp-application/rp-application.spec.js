(function () {
    'use strict';

    describe('cmRPApplication module', function () {

        beforeEach(module('cmRPApplication'));

        describe('RPApplication controller', function () {

            it('should ....', inject(function ($controller) {
                //spec body
                var dul_reviewCtrl = $controller('RPApplication');
                expect(dul_reviewCtrl).toBeDefined();
            }));

        });
    });

})();
