To run the app:

1. git pull over genomebridge-consent folder
2. npm install && bower install over consent-ui folder
3. gulp serve

If 'command not found' error appears on any of them, put npm install -g name of the one that failed (gulp or bower) and then again gulp serve