'use strict';

describe('myApp.access_review module', function() {

  beforeEach(module('myApp.access_review'));

  describe('access_review controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var access_reviewCtrl = $controller('access_reviewCtrl');
      expect(access_reviewCtrl).toBeDefined();
    }));

  });
});
