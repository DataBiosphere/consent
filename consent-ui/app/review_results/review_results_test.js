'use strict';

describe('myApp.review_results module', function() {

  beforeEach(module('myApp.review_results'));

  describe('review_results controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var dul_review_resultsCtrl = $controller('review_resultsCtrl');
      expect(dul_review_resultsCtrl).toBeDefined();
    }));

  });
});
