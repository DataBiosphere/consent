'use strict';

describe('myApp.access_review_results module', function() {

  beforeEach(module('myApp.access_review_results'));

  describe('access_review_results controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var access_review_resultsCtrl = $controller('access_review_resultsCtrl');
      expect(access_review_resultsCtrl).toBeDefined();
    }));

  });
});
