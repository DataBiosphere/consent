<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
         http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd">
    <changeSet id="91.0" author="connorlbark">
        <!--Add mapping and dataset property type columns -->
        <addColumn tableName="dataset">
            <column name="dac_id" type="bigint">
                <constraints foreignKeyName="fkDacId" nullable="true"/>
            </column>
        </addColumn>

        <addForeignKeyConstraint baseColumnNames="dac_id"
                                 baseTableName="dataset"
                                 constraintName="fkDacId"
                                 onDelete="NO ACTION"
                                 onUpdate="NO ACTION"
                                 referencedColumnNames="dac_id"
                                 referencedTableName="dac"/>

        <sql>
            UPDATE dataset d
            SET dac_id = (
                SELECT c.dac_id
                FROM consents c
                         INNER JOIN consentassociations ca
                                    ON ca.consentid = c.consentid
                WHERE ca.datasetid = d.datasetid
            )
        </sql>

        <dropColumn tableName="consents" columnName="dac_id"/>
    </changeSet>
</databaseChangeLog>
