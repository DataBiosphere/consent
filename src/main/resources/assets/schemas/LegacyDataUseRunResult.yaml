type: object
description: |
  A run and the classification counts either side of it, so a run carries its own reconciliation
  evidence. A recompute-only run must leave the counts unchanged.
properties:
  before:
    $ref: './PersistedDataUseReport.yaml'
  after:
    $ref: './PersistedDataUseReport.yaml'
  run:
    $ref: './LegacyDataUseRunReport.yaml'
