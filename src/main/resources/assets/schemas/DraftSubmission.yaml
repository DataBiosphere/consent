type: object
properties:
  storedFiles:
    type: array
    description: Uploaded files associated with the document.
    items:
      $ref: './FileStorageObject.yaml'
  createUser:
    $ref: './User.yaml'
  updateUser:
    $ref: './User.yaml'
  createDate:
    type: string
    format: date
    description: When the document was created.
  updateDate:
    type: string
    format: date
    description: When the document was last updated.
  uuid:
    type: string
    format: uuid
    description: The document UUID.
