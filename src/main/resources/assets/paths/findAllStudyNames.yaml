get:
  summary: Find All Study Names
  description: Returns a list of all the actively used study names in the backend.
  tags:
    - Dataset
  responses:
    200:
      description: dataset id
      content:
        application/json:
          schema:
            type: array
            items:
              type: string
    404:
      description: Dataset not found