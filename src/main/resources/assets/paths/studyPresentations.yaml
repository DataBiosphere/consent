get:
  summary: Registered presentations for a study
  operationId: apiStudyAssetsPresentationsGet
  parameters:
    - name: studyId
      in: path
      required: true
      schema: { type: integer }
  tags: [Study]
  responses:
    200:
      description: Study presentations
      content:
        application/json:
          schema:
            type: array
            items: { $ref: '../schemas/Presentation.yaml' }
    404: { description: Study not found, or not visible to this user }
