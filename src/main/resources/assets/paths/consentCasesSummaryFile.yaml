get:
  deprecated: true
  summary: Deprecated endpoint.
  description: Deprecated endpoint. Use  `/api/tdr/{identifier}` instead.
  tags:
    - Summary
  parameters:
    - name: type
      in: query
      description: |
        `DataAccess` (default option) or `TranslateDUL` define the type of the reviewed cases returned
      schema:
        type: string
        enum: [DataAccess, TranslateDUL]
  responses:
    404:
      description: Deprecated endpoint. Use  `/api/tdr/{identifier}` instead.
