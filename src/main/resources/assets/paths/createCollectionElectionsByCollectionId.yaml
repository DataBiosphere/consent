post:
  summary: Create Elections for Collection By ID
  operationId: apiCollectionsCollectionIdElectionPost
  description: Creates elections for the DARs in a collection. Only a DAC Chairperson is authorized.
  tags:
    - DAR Collection
    - Election
  parameters:
    - name: collectionId
      in: path
      description: DAR Collection Id
      required: true
      schema:
        type: integer
  responses:
    200:
      description: Returns target dar collection
      content:
        application/json:
          schema:
            $ref: '../schemas/DarCollection.yaml'
    400:
      description: Bad Request.  Check error details
    401:
      description: Authentication required.
    403:
      description: Forbidden. The authenticated user is not a DAC Chairperson.
    404:
      description: Not Found
    409:
      description: Conflict.  Check error details
    429:
      description: Too Many Requests - rate limit exceeded
      content:
        application/json:
          schema:
            $ref: '../schemas/ErrorResponse.yaml'
    500:
      description: Internal Server Error
