{{with $environment := env "ENVIRONMENT"}}

consent {
  baseUrl = {{if eq $environment "local"}}"https://local.broadinstitute.org:27443/"{{else}}"https://consent.dsde-{{$environment}}.broadinstitute.org/"{{end}}
}

{{end}}
