'use strict';

describe('myApp.dul_review module', function() {

  beforeEach(module('myApp.dul_review'));

  describe('dul_review controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var dul_reviewCtrl = $controller('dul_reviewCtrl');
      expect(dul_reviewCtrl).toBeDefined();
    }));

  });
});
