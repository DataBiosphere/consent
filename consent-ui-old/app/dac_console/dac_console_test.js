'use strict';

describe('myApp.dac_console module', function() {

  beforeEach(module('myApp.dac_console'));

  describe('dac_console controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var dac_consoleCtrl = $controller('dac_consoleCtrl');
      expect(dac_consoleCtrl).toBeDefined();
    }));

  });
});
